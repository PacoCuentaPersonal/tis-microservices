package com.oauth2.app.oauth2_authorization_server.application.port.in;

// Consider defining AuthResult DTO or domain object if this interface becomes active
public interface IAuthenticationService {
//    AuthResult authenticate(String email, String password);
//    AuthResult authenticateWithToken(String token);
//    void logout(String token);
//    boolean validateCredentials(String email, String password);
}
